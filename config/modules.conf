[modules]
autoload=no

; Core
load => res_musiconhold.so
load => res_stun_monitor.so
load => res_rtp_multicast.so

; Channel drivers
load => chan_sip.so

; Apps — in Asterisk 20 these are merged into app_core
load => app_dial.so
load => app_playback.so
load => app_record.so
load => app_verbose.so
load => app_userevent.so
load => app_mixmonitor.so

; Codecs
load => codec_ulaw.so
load => codec_alaw.so
load => codec_g722.so

; Formats
load => format_wav.so
load => format_wav_gsm.so
load => format_pcm.so

; PBX
load => pbx_config.so

; Functions
load => func_callerid.so
load => func_channel.so